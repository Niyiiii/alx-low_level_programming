#include <stdio.h>
/**
 * main - a program that prints the lowercase alphabet in reverse, followed by
 * a new line
 * Return: always return 0
 */
int main(void)
{
	char a;

	a = 'z';
	while
		(a >= 'b') {
			putchar(a);
			a--;
		}
	putchar('\n');
	return (0);
}
